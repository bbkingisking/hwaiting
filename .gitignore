backend/target/
backend/annyeong.db
card-lookup/

# Ad-hoc maintenance tooling (content-integrity check, conjugation-matrix
# generator) -- run by hand against a database, not part of the app or
# its build/deploy.
scripts/
# Regenerated by frontend/scripts/generate-api-types.sh - the generated
# frontend/src/lib/api-schema.d.ts IS checked in, this raw spec isn't.
backend/openapi.json

# Regenerated by CI (.github/workflows/openapi-publish.yml) and published to
# hwaitingapi.surge.sh - index.html/CNAME in backend/openapi/ ARE checked in,
# this raw spec isn't.
backend/openapi/openapi.json

# dependencies
node_modules/
.pnp
.pnp.js

# build outputs
dist/
build/
.next/
out/
.vercel/

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files
.env
.env.local
.env.*.local

# system files
.DS_Store
Thumbs.db

# IDE/editor
.vscode/
.idea/
*.swp
*.swo

# TypeScript
*.tsbuildinfo

# coverage
coverage/

# cache
.cache/
.parcel-cache/
.turbo/

# Tailwind / PostCSS
*.css.map

# shadcn/ui (generated components are usually committed,
# but ignore temp or registry cache if present)
.shadcn/

# misc
*.pem

# lockfiles (keep ONE, ignore others if not used)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
